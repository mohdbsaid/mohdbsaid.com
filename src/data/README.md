# src/data/

Static, structured, non-content data consumed by pages and components — plain `.ts`/`.json` files such as nav menus, site metadata, or link lists.

Not for long-form content (that belongs in `src/content/`), and not for reusable logic (that belongs in `src/lib/` or `src/utils/`).
