# src/assets/images/logos/

Real brand/partner logo image files (raster or vector artwork supplied by a third party) — not this site's own UI icons. This site's own icon system is `src/components/Icon.astro` (bespoke inline SVG, see docs/DECISIONS.md ADR-016) — don't duplicate that system here; this folder is only for logos that are genuinely external artwork.
