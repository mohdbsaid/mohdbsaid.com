# src/utils/

Small, pure, stateless helper functions (formatting, string/array helpers, date utilities). No side effects, no external calls.

Anything that fetches data, holds state, or wraps a third-party API belongs in `src/lib/` instead.
